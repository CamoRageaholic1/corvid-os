# =============================================================================
# Corvid OS — machineid module (fresh identifiers on the installed system)
# =============================================================================
systemd:      true
dbus:         true
symlink:      true

# Regenerate entropy/urandom seed so every install is unique.
entropy-copy: false
entropy:      false
