# =============================================================================
# Corvid OS — welcome module
# =============================================================================
# Product name/URLs come from the branding component (branding/corvid), which
# in turn is templated from branding/os-release. Do not hardcode the name here.
# -----------------------------------------------------------------------------

showSupportUrl:      true
showKnownIssuesUrl:  true
showReleaseNotesUrl: false
showDonateUrl:       false

requirements:
  requiredStorage:    15.0
  requiredRam:        2.0
  internetCheckUrl:   ""            # offline install: no connectivity gate
  check:
    - storage
    - ram
    - power
    - root
  required:
    - storage
    - ram
    - root
