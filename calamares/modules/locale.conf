# =============================================================================
# Corvid OS — locale module
# =============================================================================
# Region/zone are picked interactively; these are only fallbacks. Geo-IP lookup
# is disabled to keep the install fully offline.
# -----------------------------------------------------------------------------
region:  "America"
zone:    "New_York"

geoipUrl: ""

# Use the timezone image/map for selection.
useSystemTimezone: false
